// Defining types for our stock summary table

// Defining a type for our stock summary
export type Stock = {
  code: string,
  starting: number,
  lowest: number,
  highest: number,
  current: number
}